# PythonAnywhere-compatible dependencies (no Playwright, no APScheduler)
# Playwright cannot run on PythonAnywhere free tier (no browser support)
# APScheduler is not needed — PythonAnywhere has built-in scheduled tasks

python-jobspy>=1.1
beautifulsoup4>=4.12
httpx>=0.27
python-telegram-bot>=21.0
python-dotenv>=1.0
Jinja2>=3.1
pyyaml>=6.0

# Optional: only if you have an Anthropic API key and set llm.enabled=true
# anthropic>=0.27
